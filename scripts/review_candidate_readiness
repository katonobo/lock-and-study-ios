#!/bin/sh
set -eu

cd "$(dirname "$0")/.."
PYTHONDONTWRITEBYTECODE=1 ./scripts/validate_takken_v26_candidate
PYTHONDONTWRITEBYTECODE=1 python3 -c '
from scripts.content_review_checks import report, review_boundary_errors
raise SystemExit(report("Review candidate readiness", review_boundary_errors()))
'

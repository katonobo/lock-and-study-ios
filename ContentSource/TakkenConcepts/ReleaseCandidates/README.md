# ReleaseCandidates

明示したReviewed Master、校閲済みvariant、Golden Question Set、Free Sample profileから検証付きで作った候補だけを置きます。このディレクトリは公開Releaseではありません。

生成失敗時はファイルを作らず、既存候補を上書きしません。公開には既存のpromotion/release gateと人間承認が必要です。
